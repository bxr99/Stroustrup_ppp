Q: What is the purpose of working on the program after the first version works?
   Give a list of reasons.
A:

Q: Why does 1+2; q typed into the calculator not quit after it receives an error?
A:

Q: Why did we choose to make a constant character called number?
A:

Q: We split main() into two separate functions.  What does the new function do
   and why did we split main();
A:

Q: What is the purpose of commenting and how should it be done?
A:

Q: What does narrow_cast do?
A:

Q: What is the use of symbolic constants?
A:

Q: Why do we care about code layout?
A:

Q: How do we handle % (remainder) of floating-point numbers?
A:

Q: What does is_declared() do and how does it work?
A:

Q: The input representation for 'let' is more than one character.  How is it
   accepted as a single token in the modified code?
A:

Q: What are the rules for what names can and cannot be in the calculator program?
A:

Q: Why is it a good idea to build a program incrementally?
A:

Q: When do you start to test?
A:

Q: When do you retest?
A:

Q: How do you decide what should be a separate function?
A:

Q: How do you choose names for variables and functions?  List possible reasons.
A:

Q: Why do you add comments?
A:

Q: What should be in comments and what should not?
A:

Q: When do we consider a program finished?
A: