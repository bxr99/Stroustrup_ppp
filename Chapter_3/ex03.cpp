#include "std_lib_facilities.h"

// Program doesn't do anything, but declares a number of variables
// with legal and illegal names to see how the compiler reacts
int main()
{
  int double = 0;
  string name = 17;
  float size = 'a';
  char q = 'q';
}